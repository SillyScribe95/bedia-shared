import { saveHighNotion } from "../notionFuncs";

export function saveHighlight() {
  // function sadwe(sdfk) {
  //     // introHighlight: id,
  //   checkFullArray(people) && playlist.map(sadwe);
  //   checkFullArray(playlist) && playlist.map(sadwe);
  // function
  //     function osakw(sdofker) {
  //       bearlog.lug("___ sdofker ___", sdofker);
  //     }
  //     editModelArray(indexPlaylist, sdfk, "highlights", id);
  //     editModel(indexPlaylist, sdfk, "introHighlight", id);
  //     // addModelAttrIfNot
  //   }
}
