import { stringinarray } from "./arrayFuncs";

export function mediaTypeTrue(typeo) {
  const iewase = stringinarray(typeo, ["video", "audio"]);

  return iewase;
}

// export function savePeople(listvar){

//     checkFullArray(listvar) && listvar.map(sadwe);
    
//     function sadwe(people){

//     }
// }